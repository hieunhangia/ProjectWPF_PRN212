<Window x:Class="ProjectWPF.AdminWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectWPF.AdminWindows"
        mc:Ignorable="d"
        Title="MainWindow" Height="690" Width="960
        " Loaded="Window_Loaded">
    <Grid>
        <TextBlock HorizontalAlignment="Center" Margin="0,42,0,0" TextWrapping="Wrap" Text="Admin" VerticalAlignment="Top" FontSize="69"/>
        <Button x:Name="LogoutButton" Content="Đăng xuất" HorizontalAlignment="Left" Margin="723,55,0,0" VerticalAlignment="Top" Height="65" Width="175" FontSize="36" Click="LogoutButton_Click"/>
        <DataGrid x:Name="SellerDataGrid"
                  Margin="0,239,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  ColumnWidth="*">
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1*">
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Họ Và Tên" Binding="{Binding FullName}" Width="1*">
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Ngày Sinh" Binding="{Binding BirthDate}" Width="1*">
                </DataGridTextColumn>

                <DataGridTextColumn Header="Địa Chỉ" Binding="{Binding Address}" Width="3*">
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Hành Động" Width="SizeToHeader">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Cursor="Hand"
                                       Text="Cập Nhật"
                                       Foreground="Blue"
                                       HorizontalAlignment="Center"
                                       MouseLeftButtonUp="UpdateSellerHandler"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>

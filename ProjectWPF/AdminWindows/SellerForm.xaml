<Window x:Class="ProjectWPF.AdminWindows.SellerForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectWPF.AdminWindows"
        mc:Ignorable="d"
        Title="Thông Tin Người Bán"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#F44336"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="DarkGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#212121"/>

        <!-- Base Styles -->
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightGrayBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightGrayBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource LightGrayBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"  Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        </Style>
        <Style x:Key="ResetButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource DarkGrayBrush}"/>
        </Style>
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="LayoutTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock x:Name="SaveSellerTitleTextBlock" Text="Thông Tin Người Bán" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ForegroundBrush}" VerticalAlignment="Center"/>
            <Button x:Name="UpdateSellerAskButton" Content="Yêu Cầu Cập Nhật" Style="{StaticResource SecondaryButton}" Click="UpdateSellerAskButton_Click" Margin="20,10,0,10"/>
        </StackPanel>

        <!-- Form Fields -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 0: Email -->
                <Label Content="Email" Grid.Row="0" Grid.Column="0" Margin="10"/>
                <TextBox x:Name="EmailTextBox" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Margin="10"/>

                <!-- Row 1: Password -->
                <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="0" Margin="10">
                    <Label x:Name="PasswordLabel" Content="Mật Khẩu"/>
                    <Label x:Name="PasswordHintLabel" Content="*Để trống nếu không thay đổi" FontSize="11" Foreground="{StaticResource DarkGrayBrush}"/>
                </StackPanel>
                <TextBox x:Name="PasswordTextBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="10"/>

                <!-- Row 2: Full Name -->
                <Label Content="Họ Và Tên" Grid.Row="2" Grid.Column="0" Margin="10"/>
                <TextBox x:Name="FullNameTextBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="10"/>

                <!-- Row 3: Hometown -->
                <Label Content="Quê Quán" Grid.Row="3" Grid.Column="0" Margin="10" VerticalAlignment="Top"/>
                <Border Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" BorderBrush="{StaticResource LightGrayBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label Content="Tỉnh/Thành Phố" Grid.Row="0" Grid.Column="0" Margin="0,0,10,0"/>
                        <ComboBox x:Name="ProvinceCityComboBox" DisplayMemberPath="Name" SelectedValuePath="Code" Grid.Row="0" Grid.Column="1" SelectionChanged="ProvinceCityComboBox_SelectionChanged" Margin="0,0,10,10"/>

                        <Label Content="Xã/Phường" Grid.Row="0" Grid.Column="2" Margin="10,0,10,0"/>
                        <ComboBox x:Name="CommuneWardComboBox" DisplayMemberPath="Name" SelectedValuePath="Code" Grid.Row="0" Grid.Column="3" Margin="0,0,0,10"/>

                        <Label Content="Địa chỉ chi tiết" Grid.Row="1" Grid.Column="0" Margin="0,0,10,0"/>
                        <TextBox x:Name="SpecificAddressTextBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"/>
                    </Grid>
                </Border>

                <!-- Row 4: ID Card -->
                <Label Content="CMND/CCCD" Grid.Row="4" Grid.Column="0" Margin="10"/>
                <TextBox x:Name="IdentifyTextBox" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Margin="10"/>

                <!-- Row 5: Birth Date -->
                <Label Content="Ngày Sinh" Grid.Row="5" Grid.Column="0" Margin="10"/>
                <DatePicker x:Name="BirthDatePicker" Text="Chọn ngày" Grid.Row="5" Grid.Column="1" Margin="10"/>

                <!-- Row 6: Status -->
                <Label Content="Mở Khoá?" Grid.Row="6" Grid.Column="0" Margin="10"/>
                <CheckBox x:Name="StatusCheckBox" Grid.Row="6" Grid.Column="1" Margin="10"/>
            </Grid>
        </ScrollViewer>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="ResetButton" Content="Đặt Lại" Style="{StaticResource ResetButton}" Click="ResetButton_Click" Margin="0,0,15,0"/>
            <Button x:Name="DeleteSellerButton" Content="Xoá Người Bán" Style="{StaticResource DangerButton}" Click="DeleteSellerButton_Click" Margin="0,0,15,0"/>
            <Button x:Name="SaveSellerButton" Content="Lưu Thay Đổi" Style="{StaticResource PrimaryButton}" Click="SaveSellerButton_Click"/>
        </StackPanel>
    </Grid>

</Window>
